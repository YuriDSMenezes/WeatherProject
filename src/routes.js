import { Switch, BrowserRouter, Route } from "react-router-dom";

export function Routes() {
  return (
    <BrowserRouter>
      <Switch>
        <Route />
        <Route />
      </Switch>
    </BrowserRouter>
  );
}
